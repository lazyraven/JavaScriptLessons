//To make a single chapati, the amount of grain is consumed is equal to 100grams,
//Find out how much grains were consumed during last 10 days in my house.
//Q1. Below are the chapatis made every day.
//Q2. Total consumed grains in last 10 days

var chapatiLast10Days = [10, 12, 5, 7, 11, 9, 10, 11, 6, 7];
var chapatiLastNyt = [1, 5, 4, 10, 11];
function chapatiAmountLastDays(chapati) {
  chapati.forEach(function() {});
}
function getChapatiDays() {
  chapatiAmountLastDays(chapatiLast10Days);
}
function getChapatiNyt() {
  chapatiAmountLastDays(chapatiLastNyt);
}
